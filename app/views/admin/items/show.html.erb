<div class = "container my-4">
  <div class = "row d-flex justify-content-between">
    <div class = "text-center">
      <h2 class = "bg-light px-3">商品詳細</h2>
    </div>
  </div>

  <div class = "row mt-5">
    <div class = "col-4 text-center">
      <%= image_tag @item.get_image %>
    </div>
    <dl class="row col-8 px-5">
      <dt class="col-3">商品名</dt>
      <dd class="col-9"><%= @item.name %></dd>

      <dt class="col-3">商品説明</dt>
      <dd class="col-9"><%= @item.introduction %></dd>

      <dt class="col-3">ジャンル</dt>
      <dd class="col-9"><%= @item.genre.name %></dd>

      <dt class="col-3">税込価格（税抜価格）</dt>
      <dd class="col-9">
        <%= number_to_currency(@item.price_in_tax, unit: "円", precision: 0) %>
          （<%= number_to_currency(@item.price, unit: "円", precision: 0) %>）
      </dd>

      <dt class="col-3">販売ステータス</dt>
      <dd class="col-9">
        <% if @item.is_active %>
          <div class="text-success">販売中</div>
        <% else %>
          <div class="text-secondary">販売停止中</div>
        <% end %>
      </dd>
    </dl>
  </div>
</div>
<div class="text-center">
<%= link_to "編集する", edit_admin_item_path(@item), class: "mb-2 btn btn-success" %>
</div>

